import React from 'react'
import Dashboard from './pages/Dashboard.jsx'

/**
 * App — root component.
 * Currently renders the single Dashboard page.
 * When routing is needed, wrap with <BrowserRouter> and add <Routes> here.
 */
export default function App() {
  return <Dashboard />
}
